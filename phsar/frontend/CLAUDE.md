# Frontend — design notes

SvelteKit + Svelte 5 runes + Tailwind 4 + shadcn-svelte. Loaded on top of root [CLAUDE.md](../../CLAUDE.md) when working in the frontend tree.

## routes/

Pages: home (`/`), login (`/login`), register (`/register`), search (`/search`), media detail (`/media`), anime detail (`/anime`), ratings (`/ratings`), watchlist (`/watchlist`), settings (`/settings`), admin (`/admin`), add to library (`/library/add`).

- `GET /health` endpoint returning `{status, version}` for Coolify liveness — deliberately does not probe backend (liveness should only check what restarting this container can fix)
- `/library/add` — query-input + recent-additions panel; submitting POSTs to `/jobs/scrape` and the navbar bell takes over from there; restricted users see the page but the form is disabled
- **Tab title convention**: every route declares its own `<svelte:head><title>…</title></svelte:head>` in the format `<Page> — Phsar` (em dash, "Phsar" title case). Page name first so users with several PHSAR tabs open can distinguish them in the tab strip; the favicon already conveys "this is PHSAR". Detail pages (anime/media) bind the title reactively to the resolved name and fall back to a generic label while loading. `app.html` only sets the default `PHSAR` so the very first paint isn't blank — every route is expected to override
- **Admin tab navigation**: `/admin` uses a `?tab=` query param to switch between sections (`overview` default, `jobs`, `tokens`, `curation`, `completion`, `backups`). Tabs eager-render and stay mounted (visibility toggles via `class:hidden`), so first paint pays one parallel fetch per tab and subsequent switches are instant. The tab list, default, and validation live at the top of `routes/admin/+page.svelte`; the tab UI itself is the generic `lib/components/TabNav.svelte` (takes `tabs` / `defaultTab` / `basePath` / `ariaLabel`, reads the active tab from the URL). Adding a tab requires updating three places: `AdminTabKey` in `lib/components/admin/types.ts`, the `TABS` array + content cascade in the page, and the rendering branch
- **Ratings page tab navigation** (v0.14.12): `/ratings` mirrors the admin `?tab=` switcher (`ratings` default, `stats`); tab list + default + validation at the top of `routes/ratings/+page.svelte`, UI via the shared `lib/components/TabNav.svelte`. The page owns **one** `GET /ratings/scores` fetch and both tabs derive entirely from it client-side (no per-user stats endpoint, no extra backend math). One deviation from admin's eager-everything: the **stats tab is gated `{#if active === 'stats'}` so it remounts on each entry** (the ~6 ECharts replay their build-up animation rather than sitting idle). Filter reset is `utils/filterLifecycle`'s job, from the root layout. Component composition — list-tab views, the five stats sections — is in the `lib/components/ratings/` bullet below; the pure stats math is in `lib/utils/ratingStats.ts`. v0.15.1 added an **anime/media grain toggle** to the list tab (a pill in `RatingsFilterBar`, mirroring the watchlist bar): media grain shows one card/row per rated media (`toMediaRows`), links to the media page, and labels bands "N media"; the Statistics tab is unaffected
- **Watchlist page tab navigation** (v0.15.0): `/watchlist` uses the same `?tab=` switcher (`watchlists` default → the entries view; `stats` → the Statistics subtab; `tags` → list management — tab order is Watchlists · Statistics · Lists as of v0.15.1). The **Statistics** tab (`WatchlistStatsTab`, v0.15.1) is **presentational** — the `/watchlist` page owns both fetches (`/watchlist/items` + a lazy `/ratings/scores` fired the first time the tab opens) and computes the summary **once** at page level (a `$derived` over items + rated), so toggling grid ↔ stats does no refetch and no recompute; the tab just renders `summary`/`loading`/`hasItems` (mounting on demand replays the bar grow-in). Headline tiles (anime / media / queued time / already-rated / continuations) + top-5 genres & studios bars (studio labels link to search, genre labels carry the description tooltip, bars hover for the media main/side split + queued runtime), math in `watchlistStats.watchlistSummary`. **Continuations count only UNRATED entries** whose anime has another rated media (an already-rated watchlist entry is a rewatch, not a continuation). Restricted (guest) users **see the subtabs** (v0.15.1 — like the ratings page) with a soft muted per-tab empty-state explaining that a guest can't save/organize, instead of the old blanket "not available" Notice; the `/watchlist/items` + tags fetches still skip for guests (they'd 403). The **Watchlists** tab owns one `GET /watchlist/items` fetch (the wide per-entry projection) + a `refreshTags()`; the list tab (`WatchlistListTab`) derives both grains + both views client-side off it (`lib/utils/watchlistStats.ts`). The default (entries) tab stays mounted (`class:hidden`) to preserve scroll; the **Lists** (`tags`) tab mounts on demand and calls back to reload entries after a rename/delete/empty. Filter reset is `utils/filterLifecycle`'s job, from the root layout — the view + grain survive a detail round-trip, the value filters reset on leaving the section
- **Per-job detail page**: `/admin/jobs/[uuid]/+page.svelte` renders an update_sweep v2+ job's full audit trail — header card, counters grid, filterable media-change list (chips: all / dynamic-no-rating / rating / static / has drift; search across all title-language fields) sorted most-substantial-first via `sortMediaChanges` (group static > genre/studio > dynamic > rating-only, then by amount changed; rating-only by search-relevance impact), umbrella "Anime changes" cards (rendered **above** Media changes since v0.14.9 — far fewer rows, so they no longer get buried under 300+ media diffs). v4 (v0.14.7) renders a **Failed-refresh list card** (per-anime `step1_failures`); v5 (v0.14.8) adds a symmetric **Failed-probe list card** (`probe_failures` — title link + `error_category` + message) and a **progress-divergence warning** when `items_done < items_total` (anime selected but skipped); v6 (v0.14.9) adds an **"Attached via probe" card** (`probe_attached_anime` — anime title link + the new media titles listed below, each linked to its media detail); v7 (v0.14.14) adds a **"Removed (Hentai)" card** (`hentai_removed` — rose-tinted, titles NOT linked since the anime are deleted, + the blacklisted mal_ids). The four list cards (failed-refresh / failed-probe / attached / removed-hentai) cap at ~4 rows and scroll (`max-h-72 overflow-y-auto`) so a long list doesn't bury the changes below. **Link-target design decision (v0.14.9):** the failure + attached links navigate in the **same tab** with a `from: 'job'` origin (→ "Back to job" button, propagated across anime↔media hops) — short lists, no scroll to lose; the **Anime/Media change** cards (`AnimeUmbrellaCard` / `MediaChangeCard`) instead open a **new tab** with no origin, because that list runs 300+ rows and an admin verifying one row shouldn't lose their scroll position. A **circuit-breaker-aborted** sweep (v0.14.13) seeds its failed row with the partial `result_summary` it gathered before bailing, so a failed v2+ job renders the counters grid + the Failed-refresh (504) list **beside** the header error banner — every card here is data-presence-gated, not `status`-gated. A failed job with no partial summary shows only the banner; v1 sweeps render a "predates v0.14.5" notice. Click-through from `AdminJobsLogTab` is restricted to `update_sweep && version >= 2`; `payloadSummary()` (now in `lib/utils/jobSummary.ts`) reads media-grained keys (`media_refreshed`, `media_with_dynamic_changes`) for v5 and the anime-grained keys for v2–v4
- **Settings page** (`/settings`): restricted (guest) users see **all controls disabled, not hidden** — Rating Step, Data Export buttons, and the Spoiler Protection select (pinned to `Off`, since guests can't rate and are excluded from the spoiler cache). Theme / name-language / search-view stay editable. The server also drops a guest's `spoiler_level` update defensively (`user_settings_service`)
- **Detail-page back button**: anime/media routes render `<BackLink searchToken fromParam jobUuid />` near the top. It decides between "Back to search" (when `?q=<token>` is present, from search results), "Back to library" (when `?from=library`, e.g. recent-additions panel), "Back to job" (when `?from=job&job=<uuid>`, from the admin job-detail page's Failed-refresh / Failed-probe / Attached-via-probe links → `/admin/jobs/[uuid]`), "Back to completion" (when `?from=completion`, from the admin Completion tab's anime links → `/admin?tab=completion`), "Back to curation" (when `?from=curation`, from the Merge/Split candidate cards' anime links → `/admin?tab=curation`), and "Back to statistics" (when `?from=ratings-stats`, from a You-vs-MAL scatter point → `/ratings?tab=stats`). New origins extend the `DetailOrigin` type union in `lib/utils/navigation.ts` AND the `target` switch in `BackLink.svelte`. `buildDetailHref()` propagates `q`/`from`/`job` on internal anime↔media jumps (incl. the RelatedMediaCarousel) so a deep dive (job → anime → media → sibling) stays linkable back to the origin

## lib/components/

App components using Svelte 5 `$props()`, `$state()`, `$derived()`, `$effect()`.

- **SearchBar, MediaInfo, NavBar, TagSelect, DoubleRangeSlider, RatingCard, BulkRateDialog, AttributeSelect, RatingNeighbors, ScoreDial, DangerZone, RelatedMediaCarousel, SpoilerGuard, EChart** — core UI components
  - **TagSelect** takes an optional `labelFor?: (value) => string` (v0.15.1): the raw option/value stays in state + `onAdd`/`onRemove` (so the backend still receives e.g. `side_story`), only the shown chip/dropdown label is mapped, and the type-ahead matches on both raw + label. `SearchBar` passes `formatRelationType` for the `relation_type` filter so it reads "Side Story" / "Alternative Version" instead of snake_case; other list filters (already human-readable) leave it undefined
- **SearchBar layout + view toggle** (v0.14.13) — the filter (sliders) button overlays the input on the **left**, the search-trigger (magnifying glass, `type="submit"`) on the **right** so mobile users can tap to search instead of needing Enter. Both overlay buttons carry `z-10`: the input's `backdrop-blur` promotes it to a stacking context painted in tree order, so without it the input paints over the filter button that precedes it in source (the original right-only filter button worked only by sitting after the input). Switching anime↔media does a **partial clear** — `switchView` (`routes/search/+page.svelte`) carries only the directly-applicable filters via an allowlist (`CARRY_LIST_KEYS` = categorical lists, `CARRY_NUMBER_KEYS` = the 0–10 score range) into a fresh token and drops the view-relative ones (relation type + episodes/scored-by/duration/watch-time ranges). The `viewType` `$effect` closes the panel + refetches option bounds but does **not** `clearFilters()` — `syncFiltersFromParams` reconciles filter state from the new token. Relation Type is filtered out of the anime-view `activeFilterConfig` (per-media property, meaningless when aggregated). Landing on `/search` with **no `q` token** (the navbar link, or the ratings + watchlist "Browse anime" empty-state buttons) runs an empty (no-query, no-filter) `loadDefaultBrowse` in the default view, so the page shows a weighted-ranked default list instead of a blank page (the backend orders an empty anime query by weighted score)
- **SegmentedControl** (v0.14.12) — the on-card muted-track/solid-primary-thumb pill toggle, extracted from 4 inline copies (the ratings filter bar's any/all, the tag chart's genre/studio, the activity chart's MA-window + time-range). Generic over `T extends string | number`; presentational — the caller owns the source of truth via `value` + `onSelect`, so the highlighted `value` may be a **derived** value distinct from what `onSelect` writes (e.g. the moving-average window clamps to the dataset). Lives on the WHITE card surface, so inactive text is `card-foreground`, never the page `foreground`. `size` `sm` (next to a caption) / `md` (next to a heading). NOTE: the on-DARK grid/table view-pills in `RatingsFilterBar` are deliberately NOT this component — different surface (border-fill on dark, not track-thumb on card)
- **GrainToggle** (v0.15.1) — the on-DARK anime/media pill shared by the ratings + watchlist filter bars (extracted from two byte-identical inline copies when the ratings grain toggle landed). Presentational (`grain` + `onSelect`); a border-fill pill on the dark page surface, so — like the view pills next to it — it's deliberately NOT `SegmentedControl` (which is the on-card track-thumb toggle)
- **EChart** — the lazy-ECharts wrapper (one instance per mount, `setOption(option, true)` on every option change). **`resize()` finishes whatever animation is in flight**, so the wrapper calls it only when the container box genuinely changed (`applySize` compares against the last box it pushed) and calls it *before* applying a new option. Both halves are load-bearing for a chart whose height tracks its data: it changes `height` and `option` in the same update, the `ResizeObserver` callback lands AFTER the `$effect`, and an unguarded `resize()` there snapped the entrance animation to its end — which read as "the animation only sometimes plays", because two datasets with the same row count don't change the height and so animated fine (v0.15.3, found on the ratings Categories chart). The same mechanism is why the FIRST `setOption` is held until the observer reports a non-zero box (see the inline comment): a chart mounted in a hidden container would otherwise animate at 0×0. **`resetKey`** (v0.15.3) is the opt-in escape from `notMerge`'s transition: options are applied with `notMerge`, so ECharts animates the old series OUT while the new one animates in — right when the new option re-measures the same subject, wrong when it describes an unrelated one, where the exit tween reads as ghost marks over the new data. Pass a value that changes exactly when the subject does and the old option is `clear()`ed first. It lives here rather than in the consumer because the decision it overrides (`notMerge`) is the wrapper's, and because the alternative — remounting via `{#key}` — also re-pays `echarts.init` and blanks the plot for the initial-measurement tick. Typed **primitives only** (`string | number | boolean | null`): the check is reference identity, so an array or object literal is a fresh reference every render and would `clear()` on every option update — restarting every animation from zero, the exact inverse of the prop's purpose, with no type error and no test signal. Join a composite key into a string
- **TagBarLabel** — the bar-label shared by the ratings Categories chart + the watchlist stats bars. **Genuinely presentational since v0.15.3**: every destination is caller-owned via `onClick` (a button when set), so it knows nothing about search, the ratings filter, or which dimensions have a drill-down — the studio "other anime from this studio" jump moved out to its two call sites. The one remaining knob, `describeGenre`, only asks "look this name up in the genre-description store for a tooltip". That split matters because "is this clickable" and "does this have a description" are **independent** questions: the previous `kind: 'genre' | 'studio'` union conflated them, so season and age dimensions had to claim `kind: 'genre'` purely to dodge the studio branch — which would have had a season or age label silently inherit a same-named genre's description. The parent still owns `ensureGenresLoaded()`
- **Tooltip** — the single app-wide hover/focus tooltip (wraps the shadcn-svelte/bits-ui `ui/tooltip` primitive). Themed to the active palette: a faint primary-tinted `popover` surface with a `--primary` border + soft primary glow (see `ui/tooltip/tooltip-content.svelte`), ~500ms delay. **Self-contained** — it renders its own `Tooltip.Provider`, so it works standalone (incl. isolated component tests) with no ancestor Provider. Two modes: pass `children` for non-interactive hints (renders a `cursor-help` span trigger — status dots, labels, chart bars) or a `trigger` snippet for interactive elements (spread `props` onto your own `<Button>`/`<button>` so keyboard focus reveals it and nothing gets double-wrapped; icon-only buttons must add an `aria-label`). Optional `contentClass` styles the content bubble (e.g. `whitespace-nowrap` for a short label that shouldn't wrap near a screen edge). Note: a test that mocks `svelte`'s `setContext`/`getContext` must delegate unknown keys to the real impl, or the Provider context breaks (see `tests/media-detail.test.ts`)
  - **The three tooltip types** (pick by what the cursor is over — never style raw text as a hint):
    1. **App `Tooltip` component** (`lib/components/Tooltip.svelte`) — the themed DOM hint, for every intentional UI hint on a DOM element. Examples: `GenreBadges` (description on a badge), `ScorePercentile` (the "Top N%" pill), `RatingsFilterBar` grid-order toggle (icon button → `trigger` snippet + `aria-label`), `JobDetailCounters` cells
    2. **ECharts chart tooltip** — the in-canvas hover for any `EChart`-based chart, configured on the chart's `option.tooltip` (the app `Tooltip` can't wrap a canvas region, so charts own their hover). Spread the shared **`chartTooltipStyle`** from `lib/utils/chartTheme.ts` into every chart's `tooltip` so the surface/border/glow match the app `Tooltip` (it themes via CSS custom props — `--tooltip-surface`, `--primary` — which resolve per active theme since the ECharts tooltip is a DOM node, and bundles `confine: true` so the popup stays in the card), then add the chart's own `trigger` / `formatter`. Examples: `RatingsOverviewTimeline.svelte` (anime page, per-media score bars) and `ratings/RatingsScoreHistogram.svelte` / `RatingsAlignmentChart.svelte` (ratings stats). Spreading `chartTooltipStyle` is the single source — don't re-set its keys per chart. **A chart `formatter` returns HTML** (ECharts renders it via `innerHTML`), and Svelte's auto-escaping does NOT reach inside these hand-built strings — so any catalog-sourced text spliced into one (anime/media titles, genre/studio names) MUST go through `escapeHtml` from `formatString.ts`. Enum-mapped labels (`formatRelationType`/`formatMediaType`), numbers, and ECharts' own `marker` spans are safe; only free-form catalog strings need it
    3. **Native `title=`** — raw text-overflow reveal on dense data cells ONLY (`MediaChangeCard` / `AnimeUmbrellaCard` / search-result titles / `RatedAnimeCard` title) so they don't each become a keyboard tab stop or spin up a Provider per row
- **GenreBadges / StudioLinks / ScorePercentile** (v0.14.11) — shared detail-page chips extracted from the anime+media `+page.svelte` (both rendered identical inline markup; extraction de-duplicates and makes them testable):
  - **GenreBadges** (`genres: string[]`) — renders each genre as a `cls.badgeGenre` Badge; wraps it in the app `Tooltip` with the genre's description when one exists (else a plain badge). Descriptions come from the `genres` store (lazy-fetched once); the tooltip is a progressive enhancement, so a missing description or failed fetch just drops the tooltip
  - **StudioLinks** (`studios: string[]`) — renders the "Studio" label + one clickable `<button>` badge per studio; clicking calls `navigateToSearch({ query:'', search_type:'title', view_type:'anime', studio_name:[studio] })` so the user lands on an anime-view search filtered to that studio ("other anime from this studio"). Tested in `tests/studio-links.test.ts`
  - **ScorePercentile** (`topPercent: number \| null`) — small always-visible "Top N%" pill placed **after** the score's rating-count text (raw MAL data first, then the derived standing), color-ramped by tier (≤15% emerald / ≤50% sky / else neutral). Always-visible so mobile doesn't depend on hover; the `Tooltip` only adds the full "among the top N% … weighted by vote count" explanation (phrased as the clamped chip rank, not an inverted "higher than (100-N)%" figure — the latter overstated the best title since `topPercent` is floored at 1). `null` renders nothing. On the anime page the pill (and the `avg_score`/`avg_scored_by` beside it) reflect the **main story only** (Main + AlternativeVersion; side stories & recaps excluded — backend `RELATION_SCORE_WEIGHTS`), so the anime score tooltip on `routes/anime/+page.svelte` says "across this anime's main entries". The shared `ScorePercentile` tooltip stays grain-agnostic since the media page ranks per-media
- **RatingsOverview** — with sub-components: Stats, Timeline, Notes, Attributes. The Stats "Episodes Watched" figure credits the actual episodes watched (every status, via the same per-media basis as `watchedSeconds`) and only shows its `/ total` denominator + bar when **every rated media has a known episode total** (`episodeTotalKnown`) — otherwise just the bare count, since a still-airing on-hold (unknown total) has no meaningful denominator and "100 / 108" reads as broken
- **`lib/components/ratings/`** (v0.14.12) — the `/ratings` page's own components (see the "Ratings page tab navigation" note above for how they compose). `types.ts` (`RatingsTabKey` / `StatsSection`); the `?tab=` switcher is the shared `lib/components/TabNav.svelte`. **List tab:** `RatingsListTab` (groups + dispatches the view), `RatingsFilterBar`, `RatingsBandGrid` → `RatedAnimeCard` (default score-band cover grid), `RatingsTable` (sortable table). **Stats tab:** `RatingsStatsTab` (section shell) over `RatingsScoreHistogram`, `RatingsAlignmentChart`, `RatingsTagChart`, `RatingsAttributeAnalysis`, `RatingsActivityChart`. The reused `RatingsOverviewStats` + the attribute viz (`AttributeRadar`/`AttributeDetailBars`, prop type widened to accept `RatingScoreItem[]`) render inside the stats tab too. All ECharts spread `chartTooltipStyle` and set `emphasis:{disabled:true}`
  - **v0.14.13 stats fixes:** `ratingStep` is threaded `+page.svelte` → `RatingsStatsTab` → the alignment + activity charts, which now format the **user** score via `formatScoreWithStep` (fixes the 1-dp `4.25`→`4.3` bug); `RatingsTable`'s `malDelta` went 1 dp → 2 dp. **Activity score-trend** is now click-through: the whole plot area navigates to the media of the rating the axis-pointer is on (not only a pixel-perfect hit on the tiny dot) — wired via `EChart`'s new `onGridClick(coord)` prop (the wrapper owns the zrender-click + `containPixel` + `convertFromPixel` dance and hands the consumer DATA coordinates; the chart just snaps `coord[0]` to the nearest 1-based rating index), then `goto(buildDetailHref('media', uuid, {from:'ratings-stats'}))` → "Back to statistics" restoring the Activity section. **Cumulative watch time** extends the same solid series flat to `now` (nothing watched since → truthful plateau, no abrupt mid-axis end) and sets `grid.containLabel: true` so a >99-day y-label (`153d 4h`) isn't clipped by the fixed left gutter. `ratingSequence`'s `SequencePoint` gained `mediaUuid` for the click target
  - **v0.15.3 — the stats tab's `tags` section became `Categories`** (section **key** unchanged, so the persisted `statsSection` and the `StatsSection` type don't churn): `RatingsTagChart` now breaks down by **four** dimensions off a `DIMS` table (`{label, singular, format}`) instead of a hardcoded genre/studio pair, with a single `labelAction(tag)` deciding where each dimension's label goes. `format` exists because the buckets are keyed on the **raw filter value** (`"17"`, not `"R-17+"`) — the click-through writes it straight into `ratingsFilter`, and mapping display-label→filter-value would break the moment a label is reworded; it's applied in the HTML label column AND the ECharts tooltip, which splices the tag itself. **Season + age labels click through to the ratings grid** filtered to that bucket (`view: 'grid'`, other value filters cleared — clicking a bar means "show me this", not "narrow what I had"), which is the useful question about your own library; a **studio**'s is the opposite question ("what else did they make") so it keeps its `/search` jump, and genres stay tooltip-only. Two animation problems live here, with different fixes. (1) The chart's height tracks its row count, so a dimension swap changed `height` and `option` in the same update and the trailing `ResizeObserver` callback **snapped** the bars mid-animation — visibly intermittent, since two dimensions with the *same* row count don't change the height and so animated fine. Fixed in `EChart` (see its bullet above), because it's a property of the wrapper, not of this chart. (2) With that snap gone, the notMerge `setOption` **exit** tween became visible: the old dimension's bars animate away while the new ones grow, reading as small ghost bars overlaying the new set. Fixed with `resetKey={dim}` — `EChart`'s opt-in "replace, don't transition", which `clear()`s before applying so there's no old series to tween out. Passed for `dim` **only**: re-sorting keeps the bar count, so ECharts maps data by index and tweens lengths in place, which is already ghost-free and the better affordance (you watch the bars re-rank), and it must not restart from zero. Note the knob deliberately lives on `EChart` rather than a `{#key dim}` remount here — remounting also re-pays `echarts.init` and blanks the plot for the wrapper's initial-measurement tick, and the decision it overrides (`notMerge`) is the wrapper's
  - **v0.15.3 — Cumulative watch time reads its own span.** `paddedAxisMin` (in `chartTheme.ts` beside `chartTooltipStyle` — its `{min, max} → number` shape IS ECharts' axis-extent callback contract, so it carries no rating semantics and belongs with the other shared option fragments) replaces the auto `yAxis.min`: ECharts anchors a `value` axis at 0, so a 1-month window over a 112d→115d running total rendered as a flat line pinned to the top. `cumulativeWatchTime` also **prepends a synthetic point AT `sinceISO`** carrying the total as of the last rating before the window — the mirror of the existing flat extension to `now`, and both exist because the plateaus at either end are real. Three consequences: a clipped window starts flat at the left edge instead of mid-history at its first in-window rating; a window with **no** ratings renders a truthful plateau at the lifetime total instead of drawing nothing (the default range is `1m`, so anyone who hadn't rated in a month saw an empty chart); and a window containing the very first rating baselines at 0. A rating landing exactly on the cutoff skips the baseline — it already *is* the window-start total, and a duplicate x draws a spurious vertical stub under `step: 'end'`
- **DeleteWatchHistoryToggle** (v0.14.10) — shared opt-in "Also delete watch history" checkbox used by both the single-rating delete dialog (RatingCard) and the bulk-delete dialog (anime page). The standing explanation lives in the component; callers pass only the count sentence (`detail`) so the copy can't drift
- **AttributeSelect** (v0.14.13) — the single attribute-dropdown used by **both** RatingCard and BulkRateDialog (was duplicated inline in each — the drift source). Props: `label`, full `options`, `value`, `onChange`, `disabled`. Adds a `✕` clear button (shown when set & not disabled) to deselect back to "Not set", and **filters the `not_applicable` sentinel out of the selectable list everywhere** — `not_applicable` is auto-set-only (never user-pickable), the single rule that keeps bulk + media from drifting (closes the old bulk-shows-N/A divergence). The trigger keeps no extra right padding: the built-in chevron is in-flow (extra `pr-*` shifts it inward), so the `✕` is an absolute overlay at `right-8`, just left of it. The auto-SET side stays in RatingCard: an `$effect` over `AUTO_NA_FIELDS` (`ending_type` + `ending_quality` — neither is knowable on an unfinished watch) sets both to `not_applicable` + `disabled` on on_hold/dropped and clears them on completed (one list so the effect + the `disabled` prop can't drift). v0.14.13 also rebucketed `has_3d_animation` to `none/rare/medium/heavy` (mirroring `fan_service`'s frequency scale) and gave `ending_quality` a `neutral` midpoint — both via an Alembic enum migration (`ALTER TYPE … ADD VALUE`/`RENAME VALUE`)
- **RatingNeighbors** (v0.14.11; extracted to a shared component in v0.14.13) — the collapsible "How you rated similar titles" panel, now mounted in **both** RatingCard and BulkRateDialog (bulk rating is anime-scoped, so it gets the same `animeUuid`/`genres`/`studios`/`ageRatingNumeric` exclude+tiebreak context, wired from the anime page). On first expand it fetches `GET /ratings/scores` once and caches it; a `$derived` over the live `score` prop runs `selectRatingNeighbors` (utils) to pick up to 4 ratings from **distinct other anime** straddling the score — up to 2 at the **same** score (capped), then the rest split evenly between strictly-lower and strictly-higher (odd slot → higher) so a dense library always shows a lower row for sanity-checking, not just equals + one step up — so moving the slider re-selects with no refetch. A failed fetch leaves `neighborItems` null (not `[]`) → "Couldn't load — Try again" instead of the misleading empty state. Rows render the score via `formatScore` (rounds to 2 dp to shed float noise like `7.890000000000001`) and expand to reveal that title's attributes (the shared `attributeBadges` from `utils/ratingAttributes.ts`). In the bulk **dialog**, the body needs `min-w-0` (Dialog.Content is a CSS grid, whose items won't shrink below a `nowrap` neighbor title) so the titles truncate instead of overflowing. v0.14.13 **color-codes** each neighbor attribute against the live `currentAttributes` prop via `compareAttribute`: green/red for the 5 quality attrs (higher/lower by ordinal position), blue for a categorical attr that differs, warm cream when it matches your pick, plain grey when you haven't set it — reactive to **both** the score and the user's own selections. Rows with attributes **default to expanded** (an explicit chevron toggle overrides) so the comparison is visible without per-row clicks
- **ScoreDial** (v0.14.13) — the editable score circle + numeric input + `Slider`, extracted from byte-identical inline copies in RatingCard and BulkRateDialog (the same drift class the `AttributeSelect`/`RatingNeighbors` extractions closed — this batch had edited both copies in lockstep). Props: `bind:score` (the raw 0–10 value; the input-on-blur and the slider both write snapped values back through it), `step` (the user's rating step), `decimals` (input precision). Owns the display re-snap (`clampAndSnapScore` on the bound value) so a stored rating loaded into the edit form still shows on-step; the parent keeps its own `snappedScore` derived for save + RatingNeighbors
- **RatingCard watch-state guards** (v0.14.13) — three rules keyed off the props the media page passes (`airingStatus`, `totalEpisodes`): (1) a **not-yet-aired** media shows a `Notice` instead of the rate form for a *new* rating (an existing one stays editable; backend `CannotRateUnairedError` backstops it). (2) **"Completed" is disabled when there's no episode total** (a still-airing series can't be finished) — a new rating then defaults to On Hold, with a hint; an already-completed existing rating keeps Completed selectable so it isn't trapped (`disabled` only when `status !== 'completed'`). (3) the **episodes** field is editable only for On Hold/Dropped, clamped on blur to the episode total or `UNKNOWN_EPISODES_CAP = 2000` when unknown (mirrors the backend clamp in `rating_service._upsert_single_rating`), and `selectStatus()` fills it to the total or clears it on switch to Completed — driven by the toggle **click** (not a mount `$effect`) so it also fires when editing a saved rating. **BulkRateDialog** excludes not-yet-aired media from the rating with a yellow warning while keeping them selected (the selection also feeds the future watchlist); Save disables when nothing's left to rate
- **Rate/rewatch → remove-from-watchlist checkbox** (v0.15.1) — rating a media (or logging a rewatch) usually means it's no longer "want to watch", so an auto-checked yellow block (**RemoveFromWatchlistToggle**, the shared component below) offers to take it off the watchlist — same convention across all three surfaces, no extra popup: (single) **RatingCard** renders it inline for a **new** rating (before the Submit button; hidden when editing an existing rating — that's not a fresh watch) and inside the **rewatch confirm dialog** (so a rewatch stays one popup, not two), calling `DELETE /watchlist/media/{uuid}` + `refreshWatchlist()` when checked and the media is listed; (bulk) **BulkRateDialog** takes a `watchlistedCount` and shows "Also remove N media from your watchlist", firing `POST /watchlist/bulk-delete` over the selection after the bulk rate. All default-checked; only shown when the media(s) are actually watchlisted. No backend change — reuses the existing delete endpoints; watchlist deletes never touch the spoiler frontier
- **RemoveFromWatchlistToggle** (v0.15.1) — the shared auto-checked yellow attention block behind the three sites above (`bind:checked` + `label` + `detail` props; caller owns state + copy). Extracted so the styling + checkbox wiring can't drift across RatingCard's two uses and BulkRateDialog — the same drift-closing extraction as `DeleteWatchHistoryToggle` (which stays separate: muted styling, delete-history semantics, not watchlist)
- **MediaInfo `is_finished` badge** (v0.14.10) — anime search cards render a small emerald "✓ Complete" badge by the title when the anime is story-complete (anime cards only, never media). **No tooltip in search** — hover popups while scrolling are intrusive; the full airing-vs-story explanation lives only on the anime detail page's "Story Complete" badge (app `Tooltip`)
- **AttributeRadar, AttributeBadges, AttributeDetailBars** — attribute visualization. Two props exist purely so the share card can reuse them verbatim instead of owning a second copy of the viz:
  - **Both treat the auto-set `not_applicable` as unset**, via the shared `isAttrRated` predicate — it's an absence of an answer (the ending of a watch that never reached one), so a pill reads `--` rather than "Not Applicable"
  - **`AttributeRadar` always renders**, drawing its bare axis web with muted labels when no *quality* attribute is rated. Both consumers want that: the page shows the radar and the pills together or not at all (`RatingsOverview` still gates the whole section on `hasAnyAttribute`), and the card is a standalone artifact that should show what wasn't rated
  - `AttributeRadar onReady` — fires once the radar has painted; passing it also switches the entrance animation OFF, since a capture has no reason to wait out a grow-in it never shows
  - `AttributeBadges layout="wrap"` — the orbital variant's pills overhang their 240px box by ~50px a side, fine inside a half-width page column but not in a fixed-width card; and a `md:`-gated layout would make the same rating export differently from a phone than a desktop. The two render branches derive from **one** `showOrbit`, so a future variant can't accidentally render both. Each `PILL_STYLES` row carries both tilts, the wrap's being **much** gentler (1.5–3° vs the orbit's 3–8°): flex lays out the *unrotated* boxes, so a tilt adds ±(width · sin θ)/2 of vertical reach the row gap must absorb — 8° on a ~290px pill needs 20px a side, which is what made adjacent rows visibly collide
- **HeroIconButton** (v0.15.2) — the icon-action chrome on the anime + media heroes (padding, hover tint, `Tooltip` + `trigger` snippet, and the disable-don't-hide wiring), with the glyph as a snippet. Both `WatchlistBookmarkButton` and `ShareButton` are thin glyph suppliers over it — the hero row's actions have to stay visually aligned, so a tap-target or hover change must not be appliable to only one (the roadmap already carries an "enlarge the bookmark tap target" item)
- **Share card** (v0.15.2) — see the root CLAUDE.md "Sharing" pattern for why this is an image and not a link:
  - **Two variants, chosen by whether a rating exists** — never by account type. Rated → the **rating** card (score + radar + attribute pills), with a `SegmentedControl` in the dialog to switch to the other. Unrated → the **info** card only, no toggle. Guests are simply the case that is always unrated, so they need no special path: the `restricted` prop is gone from both wrappers, because the anime page swallows a 403 on `/ratings/anime/{uuid}` and the media page maps it to `null` — a guest already arrives with no ratings. Sharing something you haven't watched is the *point* of the info card ("this looks good"), so the button is never disabled for it
  - **AnimeShare / MediaShare** — the per-grain wrappers each page mounts (`{anime, ratings, ratingStep, nameLanguage}` / `{media, rating, …}`). They own the button, the dialog and the tooltip copy. A wrapper rather than page-level wiring for the documented reason the `AttributeSelect`/`RatingNeighbors`/`ScoreDial` extractions exist: the anime and media pages drift whenever they each keep their own copy — and mounting one component means a page can't wire the button and forget the dialog. Both are thin: the card assembly lives in `utils/shareContent`
  - **The button is never disabled — including behind the spoiler frontier.** Asking to share is asking to see, so the card can't spoil a show the viewer just requested. A frontier gate would also be unimplementable: `SpoilerGuard`'s click-to-reveal is component-internal state, so the page's `isMediaVisible` (the precomputed frontier) never flips and a disabled button could never come back
  - **utils/shareContent.ts** — `animeRatingCard` / `mediaRatingCard` / `animeInfoCard` / `mediaInfoCard` each return one whole `ShareVariantContent` (`{headerLabel, coverUrl, metaLines, badges?, body}`), pure and unit-tested (`share-content.test.ts`). Assembling the card *here* rather than in the two wrappers is what keeps the grains from drifting — the wrappers became four lines each. `body` is a **discriminated union** (`kind: 'rating' | 'info'`), so the fields of the variant you aren't rendering are absent rather than merely unused. Rules it owns:
    - **The catalog runtime, never your progress** — the meta line describes the show, so an on-hold partial would be the wrong number there
    - **The episode count sits on exactly one line**, decided by a single predicate: the status line takes it as watched-of-total whenever the rating records a count, and only then does the catalog line drop it — never duplicated, never lost on a rating predating per-status counts
    - **Not-yet-aired media are excluded from the franchise breakdown and the one-entry check.** They can't be rated, so counting an announced sequel would make a fully-watched franchise read as unfinished. The length/season meta lines deliberately keep the backend's all-media aggregates — the same numbers the anime hero shows, so the card can't disagree with the page
    - **A one-entry anime borrows the media-grain wording** instead of a relation breakdown: for a film, its own type and runtime say more than "1/1 main media". "One entry" is measured over the *aired* set, so an announced sequel can't tip a film into the franchise shape. The subtitle stays absent even here — the one place it diverges from the media card
    - **The franchise breakdown is one function with an optional numerator.** `franchiseBucketLine(media, ratings | null)`: with ratings it's the rating card's `3/5 main media`, without it the info card's `5 main media`. One function so the omit-an-empty-bucket rule can't drift — neither variant may ever advertise "0/0 recaps". On the info card it moves out of the status line and up into the meta lines, under the watch time. Labels singularize on the **displayed** total, so the rating card reads `1/1 recap` too
    - **The info card never drops a fact, it shows `--`.** A missing episode count reads `-- eps`, a missing genre/studio/age chip is a `--` chip. An image travels without the app around it, so "no studio on record" and "we forgot to render the studio" have to be distinguishable — the same argument behind the rating card's greyed attribute pills
    - **Chip and synopsis caps** (`SHARE_MAX_GENRES` 6, `SHARE_MAX_STUDIOS` 2, a trailing `+N` absorbing the rest; `SHARE_SYNOPSIS_MAX_CHARS` 820, cut on a word boundary). The card is a fixed 540×675 with **no scroll container** and `overflow-hidden` on the outer div, so an overrun doesn't grow the card — it bleeds past the white surface and silently clips the footer. Cap and clamp guard different failures and both are needed: the clamp puts an ellipsis on text that fits the box but not the line count, while the cap bounds the string, because `line-clamp` compiles to the prefixed legacy `-webkit-box` stack that the rasterizer's foreignObject pass isn't guaranteed to carry across — a dropped `-webkit-box-orient` reflows the box to a **single line**
  - **ShareCard** — the composition that gets rasterized: the app's own sandwich (themed page gradient behind a white elevated card) at a fixed CSS size, which is exactly why `AttributeRadar`/`AttributeBadges` drop in unstyled — their colors are already tuned for the white surface. Purely presentational; every string arrives resolved (title language, score precision, cover already a data URI), so the two grains differ only in what the wrapper passes. `metaLines` is a **list**, split by the caller rather than wrapped here, because only the caller knows a line has grown too long. **One component for both variants**, branching on `body.kind`, so the shell (gradient, header, cover, title, meta lines, footer) is identical by construction rather than by discipline — two exports of the same app must not read as two products. The attribute block has no gate (unlike the page's Attribute Summary), so unrated axes/pills show greyed — a standalone artifact should say what wasn't rated instead of silently dropping it. **Constrained to the foreignObject-safe CSS subset**: no `backdrop-filter`, no CSS `mask`, no scroll containers, nothing viewport-dependent. Uses the 32px favicon rather than the logo — the rasterizer re-embeds every non-`data:` `<img>` src as base64 on each capture, and the logo is 132 KB for a 28px mark
  - **The info variant's layout, and the three numbers behind it.** `text-sm` is 13px here (an `@utility` override in `app.css`), so: header 64 + footer 43.5 leaves the white card **535.5px of content at 468px wide**, and the hero row is pinned at the 176px cover. Facts band and synopsis therefore share ~347px — the same room the radar (a hard 224px) and its pill rows occupy on the rating card, which runs at 99% of budget. Three consequences that are not cosmetic: the badges under the title use **`py-0.5`**, not the hero page's `py-1` (two badge rows at the taller size overrun the cover and clip); the title pins **`text-lg`** on this variant rather than only for long titles (it carries a badge row the rating variant doesn't, and `text-xl` lands half a pixel from clipping); and the chips are a **full-width band below the hero**, not beside the cover — in the 328px hero column a heavily-tagged anime wraps to four rows, and the `max-h` sits on the chips rather than the band so an overflow eats genre chips and the studio row survives. `min-h-0` on the synopsis wrapper is load-bearing: a flex child defaults to `min-height:auto`, which defeats `overflow-hidden`. The synopsis clamp (12 lines = 234px) is sized to the **worst** band, not the common one — the heaviest catalog rows wrap their chips to two rows and leave ~245px, and clamping any looser fills the common one-chip-row card (~276px) at the cost of the heavy ones being cut mid-word by the overflow, with the clamp's ellipsis never reached
  - **The card renders plain spans, not `GenreBadges`/`StudioLinks`.** Those are page affordances a static image has no use for, and two of them are actively harmful here: `GenreBadges` calls `ensureGenresLoaded()` on mount, which would put a `GET /filters/genres` **inside the capture window** and break the "the capture performs no network I/O" invariant; and `StudioLinks` renders `<button>`s, which would be the first focusable elements inside the off-screen `aria-hidden` subtree. The studio chip also uses solid `bg-muted` rather than the page's `bg-card-foreground/8` — a fractional-alpha `color-mix` that fails to reparse in the clone degrades to invisible on a light-on-light chip
  - **ShareDialog** — owns the capture. On open: mount `ShareCard` into a `position:fixed; left:-10000px` container — **not** `display:none`/`visibility:hidden`, since `EChart` measures its container before painting and a hidden card captures blank — then await card-ready and `captureCardPng`. The **preview is the generated PNG, not the live card**, so what the user sends is exactly what they approved and Save/Share are instant. Things this got wrong once, or would get wrong, and must keep right:
    - the `$effect` wraps `generate()`/`reset()` in **`untrack`** so it depends on `open` and `variant` ALONE — both touch `previews`, and reading it made the effect re-trigger on its own output in an endless rebuild loop
    - a `generationId` guard (the detail pages' `loadRequestId` idiom) so an overlapping run — a reopen, a retry, or a variant switch mid-build — can't publish a stale preview or unmount the live run's card mid-capture
    - **one built PNG per variant, kept for the dialog's lifetime**, so toggling back is a swap and never a rebuild; each entry holds the `File` and its object URL as **one** value so no condition can see them half-set, and both are revoked on close. A cover data-URI cache keyed by source URL sits beside it, because the two variants can want different covers (a one-entry anime borrows its member's)
    - **a fresh card instance per build** (`cardMounted = false` → `tick()` → true). Both readiness signals fire once per *mount* — the radar's paint event and the info card's own — so re-rendering the existing instance across a variant switch would leave the capture waiting on a signal that never comes again
    - **cover-before-mount for the chartless variant.** The concurrency exists solely because the ECharts chunk is the long pole; with no chart there's nothing to overlap, so settling the cover first makes card-ready mean exactly "everything this card draws is painted". The info card then raises `onReady` itself from `onMount` — `tick()` → `coverImg.decode()` → one rAF. The decode is not optional: on the rating variant the radar's paint bought the cover hundreds of ms of decode time for free, and removing the chart removes that accidental buffer. Reaching the 15s `CARD_READY_TIMEOUT_MS` on an info card is a bug, not a slow link
    - two things about the share sheet: a `sharing` in-flight flag makes the button inert (`Sharing…`) while a sheet is up, because a second click can't close an OS surface and the old catch-all turned that click into a false "couldn't open the share sheet" — cleared in `reset()` too, so a promise that never settles can't wedge the button past a dialog close; and when `isIosLike() && canShare` the two actions collapse into **one** "Save or share" button, since on iOS the download reaches Files rather than Photos and both outcomes live behind the same sheet
    - **`min-w-0` on the header AND the body, `max-w-full` on the preview img** — `Dialog.Content`'s auto grid column is floored by its widest item's min-content width, and every item then inherits the widened track, so ONE unshrinkable child pushes the whole dialog past its `max-w-md` panel. Two such children live here: the `truncate`d title (a long one rendered uncut and hung the preview + buttons outside the panel — `truncate` can't substitute, since its `overflow:hidden` zeroes that floor only on a grid or flex item and the Description is neither) and the preview img, which transfers its `max-h-[55vh]` through its 0.8 aspect ratio into a min-content width that outgrows the dialog above ~910px of viewport height. `max-w-full` alone can't fix the second — a percentage max-width is indefinite during intrinsic sizing, so it shrinks the img only after the track is settled. `WatchlistDialog` carries the header half for the same reason; `BulkRateDialog` documents the body half
  - The capture target is an **unstyled inner div**, not the off-screen wrapper, so the wrapper's own positioning can't leak into the clone and shift the content out of frame
- **VersionFooter** — renders at the bottom of every page, reads `PUBLIC_APP_VERSION` from `$env/dynamic/public`
- **LoadingScreen** — themed sakura-ring loader shown during initial boot + ~1.5s logout transition
- **Notice** — shared yellow info card (rounded `bg-yellow-50` surface + `AlertTriangle` icon — solid surface so it reads on the dark body gradient)
- **BackLink** — shared back-button used by anime/media detail pages; renders nothing when neither `searchToken` nor a known `fromParam` is set (so direct-URL arrivals stay clean)
- **RelatedMediaCarousel** — sibling-media row on the media detail page. Backend (`media_search_service.get_media_detail`) returns `sibling_media` chronologically via the shared `chronological_media_key()` helper in `filter_service.py` — same key the spoiler frontier and anime-detail media table use — and a `current_position` insertion index. The carousel renders a "You are here" divider at that slot; frontend doesn't compare dates, the backend owns the ordering. On mount a one-shot `$effect` centers that divider in view (`getBoundingClientRect` math; the browser clamps `scrollLeft` so markers near either end fall back to flush-left/right) so a long chain opens at the current entry, not the far left
- **BackupsCard** — admin-only dump list
  - Create/upload/download/restore/delete with a "Current" badge on the row the DB was last restored from
  - **Restorability, not just integrity** (v0.15.3): a dump is restorable right now only if its file is intact AND it sits on the live schema. That composite is **`BackupMetadata.status`, derived server-side** (`corrupt` > `outdated` > `unknown` > `ok`) — not computed here, because the startup self-heal decides whether to take a fresh dump on the same question this card renders, and while the frontend owned the composition the two disagreed (a revision-less dump showed green `ok` while the backend had judged nothing restorable). `utils/backupStatus.ts` keeps only presentation: `DUMP_STATUS_ORDER` + `compareByRestorability` for the **"By restorability"** sort (was "By integrity" — which kept reading raw `integrity`, so a schema-outdated dump sorted among the healthy ones while showing a warning badge). Worst-first, `unknown` ahead of `outdated` since an unverified dump is probably fine whereas an outdated one definitely would roll the schema back, and an *unrecognised* verdict (server ahead of this build) sorts worst rather than best. A mismatch **replaces** the green `ok` with an amber "schema outdated" badge rather than sitting beside it (both at once reads as contradictory). Each row shows its own revision beside the size (a reference value, not a status, so it's out of the badge row), and the controls row carries the **live** revision right-aligned — from the `BackupListResponse` envelope, deliberately not derived from "whichever dump is `schema_current`", which made it unavailable exactly post-migration when no dump matches and the amber tooltip most needs something to point at
  - **Rename / pin** (pencil → inline `Input` + Save/Cancel; `PATCH /admin/backups/{filename}`): a non-empty name pins the dump against auto-retention and renders a "Pinned" badge. Pinned rows show a one-click `PinOff` action directly in the row (no need to enter edit mode); saving a blank name in the editor also unpins. `saveName(filename, name)` is the shared call behind `handleRename` (Save) and `handleUnpin` (one-click remove)
  - Refreshes are silent — `refresh()` flips `loading` only on the initial mount, so a refetch after rename/unpin/delete/restore diffs the keyed `{#each}` in place instead of collapsing to a spinner and snapping scroll to the top (same pattern as MergeCandidatesCard)
  - **Restore link**: the `is_current` row shows "Previous state saved as `{previous_state}`"; a `pre_restore` row shows "Snapshot of the state before restoring `{restored_to}`" — both derived from sidecar metadata, no client computation
  - **The restore confirm dialog states the target's verdict** when it isn't `ok` (a `Notice`, worded per `outdated` / `corrupt` / `unknown`, off a `restoreTarget` lookup by filename). Restoring a non-`ok` dump stays allowed — rolling the schema back is a legitimate admin choice — but must not be silent: the modal covers the list, so the row badge is not visible at the moment of decision. The endpoint itself is deliberately ungated; this is the whole guard
- **MergeCandidatesCard** / **SplitCandidatesCard** — admin-only review surfaces for pending merge/split candidates
  - Side-by-side anime info (with rating count + earliest aired date as visible justification for recommended A)
  - Similarity score, merge/dismiss with confirm-step, per-row "Swap A/B" button
  - Anime titles link **same-tab** with `from=curation` (→ "Back to curation" → `/admin?tab=curation`), via `buildDetailHref` — matches the Completion tab (v0.14.11; replaced the old `target="_blank"` so a verifying admin returns to the same list instead of tab-juggling)
  - **All titles render in the admin's name-language setting** (v0.14.13) via `resolveTitle` — the A/B (merge) + source (split) anime headings, the nested media titles (merge reclassification preview, split cluster members), **and** the "Dismissed decisions" rows. These were the only curation surfaces still showing raw romaji `title`; the anime summaries already carried `name_eng`/`name_jap`, the two media DTOs (`PendingReclassification`/`SplitClusterMember`) gained them backend-side to make it possible
  - Silent refreshes — keyed each-block diffs in place instead of collapsing to "Loading…"
  - Merges trigger automatic refetch surfacing cascade-resolved pairs and freshly-detected ones
  - Both embed **admin/DismissedDecisionsSection** at the bottom (a "Dismissed decisions" expander; see below), passing `currentUsername` (threaded from the admin page's `getUsername()` context) and `onResurfaced={handleRedetect}`
- **admin/CompletionStatusCard** — the **Completion** tab (v0.14.10). A manual curation surface (not a detection queue): a debounced anime search (reuses `fetchAnimeSearchResults` → `/search/anime`, with a clear-✕ button) to mark anime story-complete, plus the marked list (cover thumbnail + "marked {date} by {admin}" audit, client-side sort newest/oldest/title, unmark). Marking clears + closes the search so it reads as a committed selection. Same silent-refresh pattern as MergeCandidatesCard. Anime links navigate same-tab with `from=completion` (→ "Back to completion"). Unmark (X) is guarded by a **click-to-arm confirm** (v0.14.11): first click arms the row (X → red `Check`, tooltip "Click again to confirm removal", auto-disarms after 3s), the second click within the window fires the `DELETE` — mirrors the candidate cards' confirm-step so an accidental click can't silently un-mark. No `currentUsername` prop — the admin role gates the page
- **admin/DismissedDecisionsSection** — shared, generic (`<T extends {uuid, dismissed_at}>`) collapsible history of dismissed merge/split decisions, reused by both candidate cards. Lazy-fetches its `listUrl` on first expand; renders each row via a parent-supplied `row` snippet (merge shows A↔B; split shows the source + "would split off:" member titles per cluster). A username-gated confirm dialog (mirrors BackupsCard restore) POSTs `${basePath}/{uuid}/delete`, then calls `onResurfaced()` so the parent re-runs detection and the freed candidate pops back into the pending list. Subscribes to `curationRefresh` (`onBump`) so the counter/list re-fetch when the parent dismisses a new candidate — no stale `(N)`
- **MaintenanceBanner** — wraps a `Notice`, lives in a sticky container in `+layout.svelte` alongside the navbar
  - Polls `GET /maintenance/status` every 30s via raw `fetch` (bypasses `api.ts` so a 503 mid-window can't trigger maintenance redirect and defeat the pre-warning point — 30s instead of 60s because seasonal sweep's maintenance window can be only seconds long)
  - Subscribes to `token` store + `maintenanceRefresh` bump signal so relogin or any 503-with-maintenance response refreshes in milliseconds
  - Countdown wording: `"Scheduled maintenance starts in ~N minute(s) — pause your current episode."` within 30 min; `"Maintenance in progress. Please try again later."` when active; otherwise hidden
- **SessionTimeoutBanner** — the sliding-session driver + idle warning, mounted in the same sticky container as MaintenanceBanner (above it — a "you're about to be signed out" countdown outranks a future-window notice), guarded so it only runs while authenticated and off `/login`/`/register`. Self-contained like MaintenanceBanner: owns global activity listeners (`mousemove`/`keydown`/`scroll`/…) that just stamp `lastActivity`, a 1s tick, and a `visibilitychange` re-check (so a slept/backgrounded tab re-evaluates on resume instead of waiting for the next tick)
  - Each tick decodes the JWT `exp` and runs the pure `evaluateSession` (`lib/utils/sessionTimeout.ts`): **refresh** (recent activity + token in its back half → silent `POST /auth/refresh`, guarded by `refreshInFlight`, compare-and-swap on the `token` store so a response landing after logout/another-tab-change is dropped; a 401 from refresh is terminal), **warn** (idle + ≤3 min left → renders a `Notice` countdown `"You'll be signed out in m:ss … click your mouse to stay signed in."`), or **logout** (≤0 → calls the `onExpire` prop **once**, latched via `expired`; the layout shows its existing `TokenExpiryDialog`). `expired` resets when a fresh token arrives (login or cross-tab refresh) so the tab resurrects. Constants (`WARNING_LEAD_MS=3m`, `REFRESH_THRESHOLD_MS=5m`, `ACTIVITY_WINDOW_MS=60s`) live in the util with the invariant `REFRESH_THRESHOLD > WARNING_LEAD` so an active user never sees the banner. The layout's `token.subscribe` short-circuits its `/users/settings` refetch when the decoded `sub` is unchanged, so a silent refresh every ~5 min doesn't re-fetch settings
- **JobBell** — polls `/jobs/mine`
  - Caps dropdown at 5 entries (older spill to `/library/add`)
  - Hides retry button when `result_summary.retryable === false`
  - Disables retry across all rows while one is in flight
  - Progress bar uses `Math.min(100, Math.round(percentOf(items_done, items_total)))` — backends occasionally over-report a child batch as more items than the initial total estimate; the 100-cap keeps the bar from rendering 137%
  - **Clickable finished rows** (v0.14.13): a succeeded `user_scrape` row links to `/library/add` and a succeeded `backup` row to `/admin?tab=backups` (chevron affordance + dropdown closes on click). `rowHref(job)` gates on `status === 'succeeded'` and looks the destination up in `JOB_SUCCESS_HREF` (per-kind static metadata in `formatString.ts`, beside `JOB_KIND_LABELS`/`SEASON_SWEEP_KINDS`); rows split into an `<a>` vs `<div>` wrapper (a shared `jobRow` snippet renders the body) rather than `<svelte:element>` so the anchor is a real interactive element with no a11y-role guesswork. Failed rows keep their retry button and stay non-linked (succeeded/failed are mutually exclusive)
  - **Dismissible-while-running badge** (v0.14.13): opening the dropdown now marks **every** visible job seen (active OR finished), not just finished ones — so the badge can clear while a job is still fetching. `badgeCount` counts `unseenActive + unseenFinished + unseenCuration` (active jobs filtered against `seenUuids` like finished ones); `pollDelay` still keys off the full `activeJobs` list so cadence stays fast after the badge is dismissed
  - **Completion toasts** (v0.14.13): on an active→finished **transition** (tracked via a non-reactive `prevStatusByUuid` snapshot rebuilt each fetch) the bell fires a green/red `pushToast` (`lib/stores/toast.ts`) for `user_scrape` + `backup` only — restore (admin is hard-navigated off-page) and sweeps (system jobs) return `null` from `toastForJob`. Empty snapshot on first fetch ⇒ already-finished jobs never toast; a `toastedUuids` Set guards a double-fire across overlapping poll/`jobsRefresh` fetches. This is independent of the existing `announcedSavedUuids` bump logic (which still drives `librarySaved`/`backupSaved` page refreshes)
- **Toast / ToastHost** — `Toast.svelte` is the `fixed top-20` fly-in banner; v0.14.13 gave it a `variant` prop (`info` → `bg-primary`, unchanged default; `success` → literal emerald — no `--success` theme token exists; `error` → themed `destructive`). `ToastHost.svelte` is mounted **once** in `+layout.svelte`, renders `{#if $activeToast}{#key $activeToast.id}<Toast …/>{/key}{/if}` — the `{#key}` on the store's monotonic `id` replays the fly-in when a new toast lands mid-display (old animates out, new in) instead of swapping text silently. **`pushToast()` is the single app-wide toast path** — the bell's completion toasts, `/library/add`'s enqueue toast, `/settings` ("Settings updated"), BackupsCard ("Backup queued…"), and RegistrationTokensCard ("Copied"/"Token deleted") all go through it (v0.14.13 removed the per-page local `Toast` copies + their divergent `setTimeout` durations). `Toast.svelte` is now consumed **only** by `ToastHost` — no component owns a local `<Toast>`
- **admin/AdminJobsLogTab** — paginated jobs table with kind/status filters, seasonal_sweep child expander
  - Kind/status filters persist via the `jobsFilter` module store (`lib/stores/adminJobsFilter.ts`), **not** the URL. The store survives admin tab switches AND the round-trip through `/admin/jobs/[uuid]` — both navigate to a bare `/admin?tab=jobs`, and a mounted module store carries the filter through without re-threading it (so the detail back link stays a plain `/admin?tab=jobs`). The tab reads the store via `$derived` (drives the Select + the fetch) and writes it in the filter setters; a `$effect` on `($jobsFilter, offset)` re-fetches. Leaving /admin for anything that isn't an anime/media page clears it (`utils/filterLifecycle`), so re-entering from Settings starts clean while in-section hops and a jump to a failed job's anime page keep it. Deliberately **no** URL mirror: it's an internal tool, shareable links aren't worth the reconcile complexity, and a URL copy would resurrect a filter on browser-back after leaving — refresh-survival now comes from `persistedFilter`'s sessionStorage instead, which browser-back doesn't resurrect. Values are whitelisted against `JOB_KIND_LABELS` / `STATUS_BADGE` keys (`sanitizeKind`/`sanitizeStatus` — reused verbatim as the rehydration sanitizer); pagination offset stays in component state. Helpers unit-tested in `tests/admin-jobs-filter.test.ts`
  - Live-ticking Duration column for `running` rows (shared `formatJobDuration` + a `hasRunning` short-circuit derived so the 1s interval is quiet when nothing is in flight)
  - **Two-cadence silent poll (v0.14.13):** the tab used to fetch only on mount/filter change, so a row that was `running` at load froze forever (Duration ticked off a stale `finished_at: null`, only corrected on navigate-away-and-back). It now silently re-fetches — fast (3s) while `hasRunning`, slow idle (30s) otherwise — mirroring the JobBell cadence, so running durations freeze correctly on completion **and** jobs started elsewhere (another user, the bell) surface within ~30s. `load(silent=true)` skips the spinner + error banner (no skeleton flash); the `loadRequestId` guard already de-races overlapping fetches; expanded seasonal-sweep children aren't live-refreshed (the top-level list was the reported case)
  - Click-through to `/admin/jobs/[uuid]` ONLY for `update_sweep && version >= 2` rows — the detail page has nothing to add for other kinds, so the row stays non-clickable (no cursor, no hover, no keyboard handler) and the click affordance matches what's behind it
  - **The row-derived helpers live in `lib/utils/jobSummary.ts`** (v0.15.3), not in the component: `payloadSummary` + `unknownGenreTags` / `probeAttachedMedia` / `hentaiRemoved` / `rowTintClass`. `payloadSummary` alone branches across 5 kinds and 3 `update_sweep` schema eras, which only drifts silently while it can be exercised by clicking — extracted for the same reason as `mediaChangeSort` and the `adminJobsFilter` sanitizers, and covered by `tests/job-summary.test.ts`. Two v0.15.3 fixes came out of it: the sweep branch keys off **`SEASON_SWEEP_KINDS`** (renamed from `PARENTING_KINDS`, which named only the chevron use of a set that answers two questions) rather than a `seasonal_sweep` literal (the drift that left every `upcoming_sweep` row with a blank Detail cell despite an identical `result_summary`), and a `backup` row appends `formatBytes(size_bytes)` after the filename — the value was already in the summary and only the bell rendered it
  - Amber row tint + inline "⚠ N unknown genre tags need seeding" subline when `result_summary.unknown_genre_tags` is non-empty (v3 update_sweep only), so the admin spots seeder-update work without drilling into the detail page. v6 (v0.14.9) adds a parallel **blue** tint + "🔗 N media added via probing" subline keyed on `counters.probe_attached_media_count > 0` (amber takes precedence when a row has both); media-grained, so pre-v6 rows stay un-tinted. v7 (v0.14.14) adds a **rose** tint + "🚫 N anime removed (Hentai)" subline keyed on `counters.hentai_removed_count > 0`; rose **outranks** amber + blue (a destructive removal is the highest-signal event). The mutually-exclusive tint cascade lives in a small `rowTintClass(hentaiCount, unknownTagCount, probeMedia)` helper so precedence is explicit; the sublines below stay additive (each renders independently)
- **admin/SweepTiersCard** — admin Overview card showing where every row sits in the update-sweep cycle, with an **anime/media toggle** (v0.14.8, default anime). Takes `animeTiers` + `mediaTiers`; backend exposes 5 mutually-exclusive **cycle-membership** priority-cascade buckets (airing_now / stabilizing / weekly_cycle / archival_cycle / long_cycle) at each grain via `sweep_tiers` + `media_sweep_tiers`; the card renders the selected grain 1:1 (no fold). Membership not due-ness — counts stay stable across sweeps instead of emptying when a sweep refreshes a tier. Per-row horizontal share bar, count, percentage, tooltip with the underlying predicate (stabilize < 3 sweeps, 90-day long cycle, 180-day archival cycle for media that premiered over a decade ago — all shared by the two grains). `total` is derived from the card's own `ROWS`, so a backend bucket missing from that array silently breaks the sum-equals-catalog invariant the header copy claims — registering a new tier there is not optional. The Stabilizing row expands into per-check **sub-rows** (`stabilizing_by_check` — one per `stable_check_count` 0…threshold-1) so the stabilization pipeline is visible; bar opacity ramps with the check count (closer to graduating = more filled). Media grain buckets each media by its own count; anime grain by its **least-settled member** (MIN across its media). Sub-row count is derived dynamically (`stabilizeThreshold = Object.keys(stabilizing_by_check).length`) so retuning `SWEEP_STABILIZE_THRESHOLD` reshapes the card with no frontend edit
- **admin/JobDetailHeader / JobDetailCounters / MediaChangeCard / AnimeUmbrellaCard** — sub-components of the `/admin/jobs/[uuid]` detail page
  - `MediaChangeCard`: 5 tones (rating-zinc / dynamic-amber / static-sky / genre-fuchsia / studio-indigo) with a colored left border per row; rating sorts last so vote-count churn doesn't drown the actionable bits. Genre / studio drift folds into the same Field/Was/Now table — was-cell shows old tags with removed in red, now-cell shows new tags with added in green; v2 (legacy) drift kinds render a "Not auto-applied" subline since the dispatcher only logged them
  - `AnimeUmbrellaCard`: per-anime 7-field umbrella diff + per-media relation reclassifications + "anchor moved" / "embedding regen" badges
  - `JobDetailCounters`: takes `version`; the STATS array is version-aware — v5+ shows media-grained counters (`media_refreshed`, `anime_touched`, `media_skipped_fresh`) and drops the `anime_with_*` rollups, v2–v4 show the anime-grained originals. Most cells carry an explanatory `Tooltip`. v0.14.9 **dropped the `step1_failed` / `probe_failed` cells** (the amber "Failed refresh" / "Failed probe" cards below already carry the count, so a grid cell just duplicated the signal — and with them gone the version-gated amber-warn logic went too), and softened labels: "Anime reclassed" → "Anime changed", "Anime w/ new attach" → "Anime w/ new media"

- **Watchlist components** (v0.15.0):
  - **WatchlistBookmarkIcon** — the shared glyph, rendered by colors: `[]` → muted outline (not on the list), one color → solid fill (the media's list, or an anime under one list), several → a **gradient** (an anime whose media span multiple lists). The gradient uses a CSS `mask` of the bookmark silhouette (not an SVG `<linearGradient>`) so there are no gradient-id collisions across the many bookmarks on a page. `tagGradient` (in `utils/watchlist.ts`) builds a **hard-stop** banded gradient so each list's color stays identifiable
  - **WatchlistBookmarkButton** — the interactive hero toggle (media + anime pages); wraps the icon in the app `Tooltip` + a `<button>`. v0.15.1 added a `disabled` prop: restricted (guest) users now see it **visible-but-inert** (dimmed, "Guest accounts can't use the watchlist" tooltip) instead of hidden, so they see what their own account could do — the callers flipped from `{#if !isRestricted}` to `disabled={isRestricted}`
  - **WatchlistDialog** — single add/edit/remove for a media (list select + priority + note). "Update" is enabled only when something actually changed (`isDirty`). **BulkWatchlistDialog** — bulk add (list + priority applied to all selected media; the note goes on the first main season only, mirroring bulk rating's last-main note), an overwrite warning when any selection is already listed, and an optional side-stories checkbox — **excludes summaries/recaps** — used by the anime-hero add-all
  - **WatchlistTagSelect** (colored list dropdown) + **PriorityPicker** (High/Medium/Low) — shared by both dialogs (the drift-closing extraction pattern, cf. `AttributeSelect`)
  - `watchlist/` dir (the `/watchlist` page's own components): **WatchlistListTab** (filter → grain → view dispatch), **WatchlistFilterBar** (view grid/table, grain anime/media, multi-select list-union filter, multi-select **priority-union filter** — v0.15.1 replaced the band-order arrow; the selected chip takes the band's accent color and keeps a color-switching dot like the list chips), **WatchlistPriorityGrid** → **WatchlistCard** (cover grid grouped into priority bands; the band header mirrors the ratings band header — colored priority word + a per-grain "N anime"/"N media" count), **WatchlistTable** (sortable — incl. a **Note** column: a tick+tooltip at media grain, a count at anime grain; whole-row click via `rowClickNavigate`, the title link recolors on row hover via `group`/`group-hover`), **WatchlistTagsTab** (create/edit/delete/empty lists; inline edit puts name + color on one row with a dirty-flag Save/Cancel) + **TagColorPicker** (a color circle → a dialog with a generated **hexagon color wheel** (`buildColorWheel`, black center) + an editable hex field + Apply; the circle cycles hue on hover via an animated `@property`; blocks the reserved default-list orange)
  - **Colored bookmark everywhere**: a media/anime shows its watchlist state (colored by its list) wherever it appears — `MediaInfo` search cards (media = its single list; anime = aggregate of its media's lists via the `watchlistAnimeColors` derived store, gradient when it spans lists), the media + anime hero (`WatchlistBookmarkButton`, disabled-not-hidden for restricted users since v0.15.1), the anime media-table per-row + select-mode "Watchlist" (bulk add) / "Remove from Watchlist", the `RelatedMediaCarousel` cover overlay, and the `/library/add` recent-additions cover overlay (v0.15.1 — anime-level, so keyed on `watchlistAnimeColors`). All read the `watchlistTags` store, so a mutation anywhere re-colors every surface at once

## lib/components/ui/

shadcn-svelte base components (button, card, input, badge, slider, dropdown-menu, popover, checkbox, label, select, separator, etc.).

## lib/

- **api.ts** — centralized API client
  - Methods: `get`/`post`/`postForm`/`put`/`del`/`downloadBlob`/`postMultipart` (`downloadBlob` hands off to the shared `utils/download.ts` anchor helper)
  - `ApiError` class, automatic auth header injection from token store
  - Maintenance-503 handler: clears token, calls `bumpMaintenanceRefresh()` (covers user-on-/login where `token.set(null)` is a no-op), hard-navigates to `/login`
  - **No** global 401 handler: a plain 401 just throws `ApiError` for the caller to handle. This is deliberate — the idle-timeout path is the SessionTimeoutBanner dialog, and a global 401-redirect would let a background poll (e.g. the bell) hit a 401 on the just-expired token and hard-redirect to `/login`, defeating the dialog. The bell stops polling on its own 401; the `+layout.ts` navigation guard covers stale tokens on route changes
- **types/api.ts** — TypeScript interfaces mirroring backend Pydantic schemas (`MediaConnected`, `FilterOptions`, `TokenResponse`, etc.)
- **stores/** — Svelte stores for auth, settings, spoiler visibility, bell session, cross-component bumps
  - `jobs.ts`:
    - Three writable bumps via `createBumpStore()` factory + `onBump(store, fn)` helper:
      - `jobsRefresh` — bumped by `/library/add` or BackupsCard after successful POST so bell refetches in ms instead of waiting for 30s idle poll
      - `librarySaved` — bumped by bell when it observes a new `succeeded` `user_scrape` so `/library/add` refreshes recent-additions
      - `backupSaved` — bumped by bell on new `succeeded` `backup` so BackupsCard auto-refreshes
    - Same `announcedSavedUuids` Set covers both kinds — UUIDs are globally unique
    - `optimisticJobs` (writable list) + `addOptimisticJob(job)` / `reconcileOptimisticJobs(fetched)`:
      - Bell merges optimistic ∪ fetched (deduped by UUID) inside `$derived`
      - Callers seed a `queued` row synchronously off returned `job_uuid`; next fetch reconciles (UUID match → optimistic pruned)
    - Store-update helpers short-circuit no-ops (svelte writables notify on every `set()` even with same-reference returns)
  - `toast.ts` (v0.14.13) — global transient toast: `activeToast` writable (one slot, not a queue — a newer toast replaces the current) + `pushToast(message, variant?, durationMs=3500)`. Rendered by the single `ToastHost` in `+layout.svelte` and used by **every** app toast (bell completion toasts, library-add enqueue, settings save, backup queued, token copy/delete) — see the Toast/ToastHost component note
  - `maintenance.ts` — `maintenanceRefresh` writable + `bumpMaintenanceRefresh()`; `api.ts` bumps on every 503-with-maintenance, `MaintenanceBanner` subscribes via `onBump`
  - `auth.ts` — the `token` writable (localStorage-backed). A `window` `storage` listener syncs it across tabs (equality-guarded): when one tab slides its session forward, the others adopt the fresh token instead of logging out on a stale `exp` (which would clear the shared localStorage and kill every tab)
  - `bell-session.ts` — `BELL_LOGIN_KEY` / `BELL_SEEN_KEY` + `clearBellSession()` helper; `auth.ts` calls it when token transitions to null so logout-and-back-in doesn't inherit previous session state (sessionStorage survives hard navs)
  - `filterOptions.ts` — `ensureFilterOptions(viewType)` / `clearFilterOptions()`. Session cache for `GET /filters/options`, keyed by `view_type` (the two views return different payloads, so one slot for both would serve the wrong bounds). Copies the `genres.ts` shape — stored promise, so concurrent callers share one request and a failure evicts the key. Worth caching because the endpoint is 8–15 backend round trips for data that changes only with the catalogue, while `SearchBar` mounts on BOTH `/` and `/search`, so every hop between them refetched. Subscribes at module scope to `librarySaved` (bumped by the JobBell on a succeeded scrape) and clears — the bump can land with no SearchBar mounted, and only the next mount cares. **Not** per-user, so deliberately NOT in `clearPerUserStores`
  - `ratingScores.ts` — `ensureRatingScores()` / `invalidateRatingScores()` / `clearRatingScores()`. Session cache for `GET /ratings/scores`, the app's heaviest response, shared by its three consumers (the `/ratings` page, the watchlist Statistics subtab, `RatingNeighbors` inside RatingCard + BulkRateDialog). Coalescing alone wouldn't help — those visits are sequential, not concurrent — so it's a real cache, which means **every rating write must call `invalidateRatingScores()`**: RatingCard's upsert / delete / rewatch, BulkRateDialog's bulk rate, and the anime page's bulk delete. A missed site shows stale scores until reload, the one failure mode a per-mount fetch couldn't have. PER-USER, so `clearRatingScores()` is in `clearPerUserStores`
  - `genres.ts` (v0.14.11) — `genreDescriptions` map store (lowercased name → description) + `ensureGenresLoaded()`. Lazy-fetches `GET /filters/genres` once per session (concurrent callers share one in-flight promise) and caches the lookup for GenreBadges' tooltips; a failed fetch clears the promise so a later mount retries
  - **`persistedFilter.ts` (v0.15.4)** — the sessionStorage-backed store factory behind all three section filters. `createPersistedFilter({key, version, defaults, sanitize, preserveOnClear})` → `{store, clear, reset}`; storage is a **mirror** of the store (a subscriber writes through), so `clearXFilter()` persists its own reset with no second call. Three things carry the weight:
    - **A `{v, state}` envelope whose version mismatch DISCARDS**, and a mandatory per-store `sanitize` — rationale in the source. `pickKey` takes a `Record<Union, unknown>` rather than an array so a union member added later is a compile error at the whitelist; it's the shape `adminJobsFilter` already used against `JOB_KIND_LABELS`/`STATUS_BADGE`, whose `sanitizeKind`/`sanitizeStatus` are reused verbatim as its rehydration sanitizer
    - **A module-level registry, not a key list.** Each filter registers its own reset and `resetAllPersistedFilters()` calls them all, so a fourth filter joins logout/user-switch cleanup automatically — the failure mode `bell-session.ts`'s hand-maintained three-key list still has
    - **The subscriber skips a write whose serialized value is unchanged.** Without it the immediate first fire persists what `read` just returned (three defaults-only keys per tab, for an anonymous visitor too), and every navigation's up-to-three no-op clears each pay a `setItem`
  - `ratingsFilter.ts` (v0.14.12) — module store for the `/ratings` page (`view`/`grain`/`sort`/`sortDir`/`bandDir`/`genres`/`genreMode`/`ageRatings`/`seasons` + `statsSection`) + `clearRatingsFilter()`. Also written from the **stats** tab since v0.15.3: a season/age bar label sets `view: 'grid'` + its one value filter and navigates to `?tab=ratings`, which is why the filter surviving a within-section hop is load-bearing rather than just convenient. **Not URL** — same deliberate call as `adminJobsFilter`: internal browsing tool, `?tab=` already owns the URL. v0.15.4 moved it onto `persistedFilter` (key `phsar.filter.ratings`), so it now survives a hard refresh too and is reset by `utils/filterLifecycle` rather than a layout `onDestroy`. `clearRatingsFilter` preserves `view` + `grain` + `statsSection` (v0.15.1 added `grain` — anime/media, mirroring the watchlist toggle) so a refresh / detail round-trip doesn't lose the chosen view, grain, or which stats section you were on. `genres`/`seasons`/`ageRatings` are rehydrated unchecked on purpose — catalog values have no fixed key set
  - `watchlist.ts` (v0.15.0) — `watchlistTags` (Map media_uuid → its `{tag}`, the bookmark icon-state + color source, mirrors `spoilerVisibility`) + `watchlistAnimeColors` (derived Map anime_uuid → distinct list colors, first-seen order → solid or gradient). `refreshWatchlist()` re-fetches `GET /watchlist/media-tags` after any mutation; `clearWatchlist()` on logout
  - `tags.ts` (v0.15.0) — the user's lists (default first), cached so the dialogs preselect the default and the overview shares one source; `refreshTags()` / `clearTags()`
  - `watchlistFilter.ts` (v0.15.0) — module store for the `/watchlist` list controls (`view` grid/table, `grain` anime/media default anime, `tagUuids` union, `priorities` union, `sort`/`sortDir`) + `clearWatchlistFilter()` — same not-URL rationale as `ratingsFilter`, and since v0.15.4 the same `persistedFilter` backing (key `phsar.filter.watchlist`); preserves view + grain across a detail round-trip, resets the value filters. Its `sanitize` deliberately leaves `tagUuids` unchecked — `WatchlistFilterBar` already prunes uuids missing from the loaded `tags` store, so one pruning site, not two that can disagree. v0.15.1 replaced the `bandDir` order arrow with the `priorities` multi-select filter (bands now always run High-on-top)
  - **Per-user store lifecycle** (`routes/+layout.svelte`): watchlist + tags (and spoiler visibility + settings) load on login and clear on logout **AND on a real user switch** (A→B without a null-token transition — the login page sets the new token directly), so B never inherits A's bookmarks. Restricted users skip the watchlist/tags fetches (they'd 403). `clearRatingScores()` joins them too (it holds one user's entire rating set). Since v0.15.4 `resetAllPersistedFilters()` joins both paths — logout in `auth.ts` beside `clearBellSession()`, the user switch in the layout's `isUserSwitch` branch — because the filters are now per-tab sessionStorage, and `watchlistFilter.tagUuids` holds A's list uuids, which B can't even see
- **utils/** — helpers:
  - String formatting: `formatAiringStatus`, `formatRelationType`, `formatMediaType`, `formatSeasonRange`, `formatDuration`, `formatDurationCompact` (tight axis ticks — a *contiguous* most-significant run, so 1d 0h 1m reads "1d", not a misleading "1d 1m"), `formatJobDuration`, `formatDecimalDigits`, `roundScore` (round a 0–10 rating to 2 dp — the finest step is 0.01 — to shed float-arithmetic noise like `535 * 0.01 = 5.3500000000000005`; the shared guard behind `formatScore` AND `clampAndSnapScore`, and the thing to wrap a stored rating in before measuring its `decimalPlaces` — never call `decimalPlaces` on a raw stored rating or one noisy legacy value reports 16 dp and blows out the display), `formatScore` (display a 0–10 rating: `roundScore` then show only the decimals that remain — `7.890000000000001`→`7.89`, `8.5`→`8.5`, `10`→`10`), `formatScoreWithStep(value, ratingStep)` (v0.14.13 — the **step-aware** display rule: at least the step's decimals so a 0.5-step user reads `8.0` not `8`, expanded when the value needs more — `formatScoreWithStep(4.25, 0.5)`→`4.25`; the single home for the `Math.max(decimalPlaces(step), decimalPlaces(roundScore(v)))` expression the ratings list/table, the anime page, `RatingsOverviewTimeline`, and the two stats charts all now share — a chart hardcoding 1 dp was the `4.25`→`4.3` bug), `formatShortDate`, `formatShortDateTime`, `formatBytes`, `formatEpisodeCount` / `episodesWatchedLabel` ("5/24 eps", dropping the denominator on an unknown total) / `watchStatusLabel` (v0.15.2 — each had been hand-copied at 2–4 sites before the share card would have added another), `isSeasonRange` (whether `formatSeasonRange` renders two seasons rather than one — card layout hangs off it, so it lives beside the formatter it must agree with), `escapeHtml` (HTML-escape catalog strings before splicing into an ECharts tooltip `formatter` — see the tooltip-types note above)
  - Numeric: `percentOf(value, total)` — share-of-total with divide-by-zero guard; callers compose with `Math.round` (integer %) or `.toFixed(1)` (decimal %). `isRatingField(name)` — single source of truth for the score / scored_by sub-bucket split used by `MediaChangeCard.dynamicTone` and the route page filter chip
  - Season logic, search params (`fetchSearchResults`, `fetchAnimeSearchResults`), navigation (`navigateToSearch`, `buildDetailHref`, `searchByStudio` — the "other anime from this studio" jump used by `StudioLinks` + the ratings tag chart; `rowClickNavigate(e, href)` — whole-row click → `goto`, preserving native new-tab (modifier/middle click) and inner `<a>` clicks, shared by the ratings + watchlist tables; `DetailOrigin` gained `'ratings-stats'` and `'watchlist'` — a watchlist entry → detail returns via "Back to watchlist")
  - `watchlist.ts` (v0.15.0) — shared watchlist constants + helpers: `PRIORITY_OPTIONS` / `priorityLabel` / `PRIORITY_ACCENT`, `tagGradient(colors)` (solid for one color, hard-stop banded gradient for several), `defaultNewTagColor()` (the new-list starting color — derived from a `buildColorWheel()` cell so the picker can always re-select it; a hardcoded hex off the wheel couldn't be restored without a refresh. A **function** since v0.15.4: as a const it built all 37 cells at module scope, and this module is imported by every surface that renders a bookmark, so each paid for a value only the color picker reads) + `RESERVED_DEFAULT_TAG_COLOR` (mirrors backend `tag_service.DEFAULT_TAG_COLOR`; the picker blocks a custom list from it)
  - `color.ts` (v0.15.0) — pure color helpers for the tag picker (unit-tested in `color.test.ts`): `hslToHex`, `normalizeHex` (validate/lowercase `#rrggbb`, mirrors the backend `^#[0-9A-Fa-f]{6}$` rule), `contrastText` (perceived-luminance → black/white glyph), `buildColorWheel` (37-cell honeycomb HSL wheel, near-black center → light rim)
  - `relations.ts` (v0.15.0) — the shared "main story" spine: `MAIN_RELATIONS` (`main` + `alternative_version`) + `mainSideLabel(main, side)` ("X main · Y side", 0 counts omitted), single-sourced here and consumed by both `ratingStats` + `watchlistStats` (each previously held a divergent copy)
  - `watchlistStats.ts` (v0.15.0) — pure `/watchlist` list-tab helpers (unit-tested in `watchlist-stats.test.ts`): the normalized `WatchlistRow` (carries `mainSide` + `noteCount` for the table's bracket + Note columns) + `filterByTags` (list-union) / `filterByPriority` (v0.15.1 — priority-band union, applied on the normalized rows so it matches the band each row displays in — anime grain filters on the anime's most-urgent priority) / `toMediaRows` / `toAnimeRows` (aggregate an anime's watchlisted media: most-urgent priority, distinct list colors, main/side counts, count of noted media) / `toPriorityBands` / `sortRows` (title/priority/date/note; the title tiebreak stays ascending so equal-key rows keep a stable order when the direction flips) + `watchlistSummary` (v0.15.1 — the Statistics tab's overview: totals, queued runtime Σ `total_watch_time` (the backend hybrid, shipped on `WatchlistItem` — no client-side episodes×duration), already-rated + continuations against `/ratings/scores` (continuations = UNRATED entries whose anime has another rated media), and top genres/studios counted per **distinct anime** with per-tag main/side media + queued seconds for the bar hover). `MAIN_RELATIONS`/`mainSideLabel` come from the shared `relations.ts`
  - Chart colors: `CHART_COLORS`, `scoreColor`, `getThemedChartColorPalette`, `RELATION_TYPE_ORDER`, `RELATION_TYPE_COLORS`, `RELATION_TYPE_LABELS`
  - `chartTheme.ts` (v0.14.12) — shared ECharts option fragments: `chartTooltipStyle`, the single themed tooltip style object (surface/border/glow matching the app `Tooltip` via CSS custom props + `confine:true`), spread into every chart's `option.tooltip` (see the tooltip-types note above); `scoreGaugeOption`; and `paddedAxisMin` (v0.15.3) — an axis floor for a series whose variation is small next to its absolute value (a cumulative total), passed straight as `yAxis.min` since `{min, max} → number` is ECharts' own axis-extent callback contract. Tested in `tests/chart-theme.test.ts`
  - `ratingStats.ts` (v0.14.12) — all the `/ratings` page math, pure + unit-tested (`tests/rating-stats.test.ts`): `groupByAnime`/`toMediaRows` (v0.15.1 — the media-grain row builder: one `AnimeRatingRow` per rated media, same shape as `groupByAnime` so the grid/table/card render both unchanged, carrying `media_uuid` for the grain-aware detail link + list key and a `relationLabel` shown in place of the anime grain's main/side count)/`filterItems`/`sortAnimeRows`/`toScoreBands` (list), `scoreHistogram` (fixed 0.5-wide buckets, disjoint + upper-inclusive — a boundary score snaps DOWN to the lower bucket), `alignmentPoints` + `weightedLinearFit` (weights `log10(scored_by+1)`) + `spearman`, `tagMetrics` (anime-level count + watch-time-weighted score; v0.15.3 widened `TagDim` to genre/studio/**season**/**age rating** via a `TAG_DIM_KEYS` extractor table — genres/studios are already many-per-media, season and age yield a one-element list, and a media carrying no value yields **none** so a null is skipped rather than bucketed as its own "unknown" tag), `attributeCorrelations` (quality scales, Spearman `|ρ|`) + `attributeCategoryEffects` (categorical choices, sample-weighted eta), `ratingSequence`/`movingAverage`/`cumulativeWatchTime` (Activity). `watchedSeconds(item)` (v0.14.13) is the shared watch-time basis — actual time watched = `episodes_watched × per-episode duration` (read off the `duration_seconds` field directly, not `total_watch_time / episodes`, so an unknown-total airing show still credits its watched time), clamped to the episode total, completed→full-run fallback, 0 when the runtime is unknown; `totalWatchTime`/`cumulativeWatchTime`/`tagMetrics` credit **every** watch status through it (on-hold/dropped partials included), where they previously summed completed-only full runtimes. `RatingScoreItem` accordingly carries `episodes_watched` + `duration_seconds` and dropped `total_watch_time`. No scipy — every fit/correlation is closed-form in TS. `meanScore(ratings)` (v0.15.2) is the plain mean behind an anime's headline "your score", shared by `RatingsOverview` and `ShareCard` so the number on the exported PNG can't drift from the number on the page
  - `jobBadges.ts` — `STATUS_BADGE: Record<JobStatus, string>` shared between `AdminJobsLogTab` and `JobDetailHeader` so status pill colors can't drift
  - `backupStatus.ts` (v0.15.3) — display ordering for the server's dump-restorability verdict (`DUMP_STATUS_ORDER` + `compareByRestorability`, tested in `tests/backup-status.test.ts`). The verdict itself is `BackupMetadata.status`, derived server-side — see the BackupsCard bullet for why composing it per-consumer was the bug
  - `jobSummary.ts` (v0.15.3) — the pure `AdminJobResponse` → display helpers behind the admin Jobs Log rows (`payloadSummary` + `unknownGenreTags` / `probeAttachedMedia` / `hentaiRemoved` / `rowTintClass`), extracted from `AdminJobsLogTab.svelte` so the kind- and version-dispatching is unit-testable (`tests/job-summary.test.ts`). See the AdminJobsLogTab bullet for what that extraction caught
  - **`filterLifecycle.ts` (v0.15.4)** — when a section's list filter is reset. The rule: **clear section S when navigating to a page that is neither inside S nor a detail page**; a detail page is a *detour*, not a destination. A `SECTION_FILTERS` prefix→clear registry + a pure `sectionsToClearFor(to)` selector, dispatched from one `afterNavigate` in `routes/+layout.svelte`. Four points that aren't obvious from the code:
    - **It cannot live in the section layouts, which is why all three were deleted** — the source JSDoc has the argument, along with why SvelteKit's `snapshot` isn't the deeper mechanism here
    - **`afterNavigate`, not `beforeNavigate`.** A section is only ever cleared when the destination is *outside* it, so no destination can read a store the call touches — there is nothing to settle first. Clearing beforehand instead invalidates the outgoing page's still-mounted derived chain and re-renders the whole grid, expanding it back to unfiltered, one tick before it unmounts. Skips `type === 'enter'`: a first load is not a navigation away from anything, and treating it as one would wipe the other sections' rehydrated filters on every refresh
    - The detour set derives from `navigation.ts`'s `DETAIL_TYPES`, the same union `buildDetailHref` is typed on, so a third detail grain can't be added to one and forgotten in the other — and the symptom of forgetting is exactly the bug this exists to fix
    - Prefix matching is exact-or-`/`-delimited, so `/ratings` never swallows a future `/ratings-archive`, and `/admin/jobs/[uuid]` still counts as inside `/admin`
  - `spoilerFrontier.ts` — client-side frontier for detail pages
  - `ratingAttributes.ts` — `attributeBadges(item)` → `{key,label,value}[]` for the filled-attribute display (skips unset + `not_applicable`); shared by RatingCard's filled badges and RatingNeighbors' rows. Also `compareAttribute(key, neighborValue, currentValue)` → `higher`/`lower`/`differs`/`match`/`neutral` + the `QUALITY_ATTR_KEYS` set (the 5 ordered attrs) powering the neighbor color-coding — `match` (warm cream) = the neighbor agrees with your pick, `neutral` (grey) = you haven't set it (kept distinct so a real match doesn't read like an unset attr), quality attrs compare by ordinal position, the rest by equality (unit-tested in `rating-attributes.test.ts`). Also `hasAnyAttribute(ratings)` (v0.15.2) — "is there a real attribute value in this set" (the auto-set `not_applicable` doesn't count), the gate behind `RatingsOverview`'s Attribute Summary **and** the `/ratings` stats tab's Attributes section (which held a byte-identical inline copy). The share card deliberately has no such gate — see ShareCard
  - `shareImage.ts` (v0.15.2) — the share-PNG plumbing: the `SHARE_PNG_*`/`SHARE_CARD_*` size pair (card laid out at exactly half the 1080×1350 output so the capture scale is an integer 2), `shareFileName(title, kind)` (slug with a CJK-only fallback; the `-rating` suffix is dropped for an info card, since the filename shouldn't promise a score the image doesn't carry), `fetchImageAsDataUri` (inlines the cover **before** the capture so the capture itself does no network I/O and its timing stays deterministic; bounded by an 8s abort so a stalled CDN response can't hang the build — the 15s card-ready ceiling starts later and never covers this fetch; `null` on any failure → the card's "No image" placeholder, never a failed export), `captureCardPng` (`modern-screenshot`'s `domToBlob`, **dynamically imported** for the same reasons as the ECharts singleton — browser-only, and jsdom has no canvas, so the module around it stays unit-testable; `font: false` since the app ships no webfonts), plus `canShareFiles`/`shareFile` (Web Share API Level 2, feature-detected) and `isIosLike`. Two notes on the sheet: `shareFile` swallows **both** `AbortError` (the user dismissing it) and `InvalidStateError` (a sheet is already open) — the latter is the backstop under the dialog's own in-flight guard, for platforms that settle the previous share's promise late or never. There is no third option of closing the sheet from script: it's an OS surface and the Web Share API has no cancellation path, which is why the dialog makes its button inert instead. `isIosLike` is a deliberately narrow UA check (iPhone/iPad/iPod, plus iPadOS which reports a Mac UA and is separated only by `maxTouchPoints`) — no feature detection exists for the thing that matters, that an `<a download>` on iOS lands in Files and only the sheet's "Save Image" reaches Photos. Pure helpers unit-tested in `share-image.test.ts`
  - `download.ts` (v0.15.2) — `triggerBlobDownload(blob, filename?)`, the throwaway-anchor dance extracted from `api.downloadBlob` so the server-authored files (exports, backups) and the in-page share PNG share one implementation; they differ only in where the Blob comes from
  - `ratingNeighbors.ts` — `selectRatingNeighbors(items, score, ctx)` for the RatingNeighbors consistency helper (RatingCard + BulkRateDialog): pool is **completed** ratings from **other** anime (dropped/on-hold excluded — biased/provisional scores), deduped to one rating per anime (closest to score); returns up to 4 ratings straddling the score — up to 2 at the **same** score (capped), then the remaining slots split evenly between strictly-lower and strictly-higher (odd slot → higher), a short side backfilling the other — each tiebroken by attribute-count → shared-genre → shared-studio → age-distance → recency. Pure so it recomputes per slider move and is unit-tested (`rating-neighbors.test.ts`)
  - `mediaChangeSort.ts` — `sortMediaChanges` for the job-detail media-change list (category-priority then amount-changed; rating-only by weighted `score*log10(scored_by+1)` delta — matches the backend search ranking). Decorate-sort-undecorate so the per-row key is computed once per re-sort; extracted from the page so it's unit-testable
  - `sessionTimeout.ts` — pure `evaluateSession({exp, now, lastActivity})` → `'logout' | 'refresh' | 'warn' | 'idle'` + `formatCountdown(ms)` + the timing constants. The testable core of SessionTimeoutBanner (see the component); "idle" is just `!activeRecently`, so an idle user never enters the refresh branch and the token decays into the warning + logout. Unit-tested exhaustively at the branch boundaries in `session-timeout.test.ts`. Also exports **`isSessionLive(exp, now)`**, the single expiry rule: `evaluateSession` uses it for its logout branch and the root layout's navigation guard uses it as its whole decision, so the two can't disagree on what "expired" means
- **themes.ts** — centralized theme config: `THEMES` record mapping keys to CSS classes, character pics, labels; `ThemeKey` type; helpers: `isValidTheme`, `getThemeCssClass`, `getThemePic`, `getThemeFocal`, `getActiveTheme`
- **echarts.ts** — lazy-loaded ECharts singleton (`getEcharts()`) using pre-built ESM bundle (SSR-safe, cached)
  - `EChart.svelte`'s **`onReady`** prop (v0.15.2) fires once the canvas holds its finished pixels. There is no DOM-observable moment for that — the first `setOption` waits on an async `ResizeObserver` measurement and then animates — so a consumer needing painted pixels (the share-card capture) can't just wait a frame. Backed by ECharts' own **`finished`** event (latched to one call), registered in `onMount` beside the click wiring rather than in the option `$effect`: that keeps the signal truthful whether the chart animates or not, and keeps the callback prop out of the effect's dependency set (an inline arrow would otherwise re-trigger a full `notMerge` `setOption` on every parent update)
- **config.ts** — backend API base URL (consumed only by `api.ts`)

## src/app.css

Theme system: `@property` definitions for `--primary`/`--ring`, `@theme inline` with `var()` indirection, `.theme-red`/`.theme-blue`/`.theme-green` override classes, themeable gradient variables (body dark gradient `--gradient-*` + auth-page light gradient `--auth-gradient-*`). Light elevated surfaces on dark gradient background. Dark mode locked to class-based only.

## tests/

Vitest + @testing-library/svelte component tests.

- `setup.ts` registers a global `afterEach` that flushes bits-ui's body-scroll-lock cleanup: a Dialog/Popover unmount schedules a ~24ms real `setTimeout` to restore `<body>` styles, and if that's the last thing a test file does it can fire after jsdom teardown → an uncaught `document is not defined`. The hook waits ~30ms **only** when a lock is still pending (`document.body.style.overflow === 'hidden'`), so non-dialog tests pay nothing. It runs after Testing Library's auto-unmount (setup files register their hooks earliest; vitest runs `afterEach` LIFO)
